<?php
declare(strict_types=1);

// טעינת ספריות לניהול סשן ופונקציות עזר
// מאפשרות גישה לפונקציות סשן והחזרת תגובות JSON
require_once __DIR__ . '/../lib/session.php';
require_once __DIR__ . '/../lib/utils.php';

// בדיקה שהמשתמש מחובר
// כדי למנוע שימוש בפונקציה למשתמשים לא מחוברים
require_auth();

// ביצוע יציאה מהמערכת בצורה בטוחה
// סגירת הסשן והסרת עוגיות שמזהות את המשתמש
logout_user();

// החזרת תגובה המאשרת שהמשתמש התנתק
// מאפשרת לממשק לדעת שהפעולה הצליחה
json_ok(['logged_out' => true]);
